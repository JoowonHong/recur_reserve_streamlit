# 🎬 예약 시스템 사용 가이드

네, 이 예약 시스템을 사용하실 수 있습니다! 아래 가이드를 따라해 보세요.

## ✅ 시스템 요구사항

- Python 3.8 이상 (권장: Python 3.10 또는 3.11)
- 인터넷 연결 (패키지 설치용)

## 🚀 설치 및 실행 방법

### 1단계: 필수 패키지 설치

터미널(명령 프롬프트)을 열고 프로젝트 폴더로 이동한 후 다음 명령어를 실행하세요:

```bash
pip install -r requirements.txt
```

### 2단계: 앱 실행

```bash
streamlit run app.py
```

실행하면 자동으로 웹 브라우저가 열리며 `http://localhost:8501` 주소에서 앱을 사용할 수 있습니다.

## 📖 주요 기능 사용법

### 일반 예약 만들기 (메인 페이지)

1. **시작 날짜/시간 선택**
   - 예약을 시작할 날짜를 선택합니다
   - 시작 시간을 선택합니다

2. **종료 날짜/시간 선택**
   - 예약을 종료할 날짜를 선택합니다
   - 종료 시간을 선택합니다

3. **예약 생성**
   - "예약 생성" 버튼을 클릭하면 예약이 저장됩니다
   - 총 예약 시간이 자동으로 계산됩니다 (일/시간/분 단위)

4. **예약 확인 및 관리**
   - 생성된 예약 목록에서 모든 예약을 확인할 수 있습니다
   - 각 예약 옆의 "삭제" 버튼으로 예약을 삭제할 수 있습니다

### 반복 예약 만들기 (스케줄 페이지)

좌측 사이드바에서 "Schedule" 페이지로 이동하세요.

1. **스케줄 이름 입력**
   - 예: "매주 월수금 운동"

2. **반복 요일 선택**
   - 예약을 반복할 요일을 선택합니다 (월~일)
   - 여러 요일 선택 가능

3. **스케줄 기간 설정**
   - 스케줄 시작 날짜
   - 스케줄 종료 날짜

4. **예약 시간 설정**
   - 매회 예약의 시작 시간
   - 매회 예약의 종료 시간

5. **스케줄 생성**
   - "스케줄 생성" 버튼 클릭
   - 선택한 요일에 자동으로 예약이 생성됩니다

6. **스케줄 관리**
   - 활성화/비활성화: 스케줄을 일시 중지하거나 재개
   - 삭제: 스케줄과 관련된 모든 예약 삭제

## 💡 사용 팁

- **다일간 예약**: 하루 이상 걸치는 예약도 가능합니다 (예: 2일 밤낮 예약)
- **자동 저장**: 모든 예약은 자동으로 데이터베이스에 저장됩니다
- **예약 시간 계산**: 시작과 종료 시간을 입력하면 총 예약 시간이 자동 계산됩니다

## 🗄️ 데이터 저장

예약 정보는 다음 데이터베이스 파일에 저장됩니다:

- `reservations.db`: 일반 예약 정보
- `schedule_reservations.db`: 스케줄 예약 정보

이 파일들은 프로젝트 폴더에 자동으로 생성됩니다.

## ⚠️ 보안 주의사항

**이 앱은 로컬 개발 환경을 위해 설계되었습니다.**

현재 설정(`.streamlit/config.toml`)에서는 CORS 및 XSRF 보호가 비활성화되어 있습니다.
- ✅ 개인 컴퓨터에서 로컬 사용: 안전
- ❌ 공개 인터넷에 배포: **권장하지 않음**

만약 인터넷에 공개하려면:
1. `.streamlit/config.toml`에서 `enableCORS = true` 설정
2. `.streamlit/config.toml`에서 `enableXsrfProtection = true` 설정
3. 추가 인증 시스템 구현 고려

## ❓ 문제 해결

### 앱이 실행되지 않는 경우

1. Python 버전 확인: `python --version`
2. 패키지 재설치: `pip install -r requirements.txt --upgrade`
3. 포트 충돌 시: `streamlit run app.py --server.port 8502`

### 예약이 저장되지 않는 경우

- 데이터베이스 파일에 쓰기 권한이 있는지 확인하세요
- 폴더 권한을 확인하세요

## 📞 추가 도움이 필요하신가요?

더 궁금한 점이 있으시면 GitHub Issues에 질문을 올려주세요!

---

**이 시스템을 자유롭게 사용하실 수 있습니다!** 🎉
